import * as React from 'react';

const Logo: React.FC = () => {
	// const navigate = useNavigate();
	return (
		<></>
		// <Box>
		// 	<AdbIcon sx={{ mr: 1 }} />
		// 	<Typography
		// 		variant="h6"
		// 		noWrap
		// 		components="a"
		// 		onClick={() => { navigate('/'); }}
		// 	>
		// 		LOGO
		// 	</Typography>
		// </Box>
	);
};

export default React.memo(Logo);
