import * as React from 'react';

const NavMenuMD: React.FC = () => {
	// const navigate = useNavigate();
	return (
		<>
			{/* <Logo /> */}
			{/* <Box sx={{ flexGrow: 1, display: { xs: 'none', md: 'flex' } }}> */}
			{/*	{NAV_LINKS.map(({ label, to }) => ( */}
			{/*		<Button */}
			{/*			key={to} */}
			{/*			onClick={() => { navigate(to); }} */}
			{/*			sx={{ my: 2, color: 'white', display: 'block' }} */}
			{/*		> */}
			{/*			{label} */}
			{/*		</Button> */}
			{/*	))} */}
			{/* </Box> */}
		</>
	);
};

export default NavMenuMD;
